{
  "name": "mindbreaker-web",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc -w",
    "build:prod": "rimraf dist/js/* dist/*.js dist/*.js.map && tsc && workbox generateSW workbox-config.js",
    "start": "http-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^3.8.3",
    "workbox-cli": "^5.1.1"
  },
  "dependencies": {
    "http-server": "^0.12.1"
  }
}
